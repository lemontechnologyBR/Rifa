<%- include('../partials/header') %>

<div class="max-w-lg mx-auto px-4 py-16 text-center animate-fade-up">
  <div class="w-20 h-20 rounded-3xl bg-gray-100 dark:bg-gray-800 flex items-center justify-center mx-auto mb-6">
    <i data-lucide="file-question" class="w-10 h-10 text-gray-400"></i>
  </div>
  <h1 class="text-3xl font-extrabold dark:text-white mb-2"><%= titulo %></h1>
  <p class="text-gray-500 dark:text-gray-400 mb-8">A página que você procura não existe ou foi removida.</p>
  <a href="<%= typeof tenantBase !== 'undefined' ? tenantBase : '/' %>" class="inline-flex items-center gap-2 btn-brand font-semibold px-6 py-3 rounded-xl shadow-md">
    <i data-lucide="home" class="w-5 h-5"></i> Voltar ao Início
  </a>
</div>

<%- include('../partials/footer') %>
